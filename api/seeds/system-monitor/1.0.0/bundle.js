var systemMonitor = { id: 'system-monitor', version: '1.0.0', run: async function(params, ctx) { return { ok: true, reading: { cpu: Math.round(Math.random() * 100), mem: Math.round(Math.random() * 100) }, at: new Date().toISOString() }; } };
