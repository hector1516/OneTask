{
  "name": "Host Manager VMware",
  "description": "Configura el archivo hosts de Windows. Si el cliente esta en la red local (ping a 192.168.1.65), usa esa IP. Si no, obtiene la IP publica del servidor.",
  "entry": "bundle.js",
  "minCoreVersion": "0.1.0",
  "permissions": ["system.read"],
  "configSchema": {
    "type": "object",
    "properties": {}
  }
}
