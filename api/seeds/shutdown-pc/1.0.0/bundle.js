var shutdownPc = { id: 'shutdown-pc', version: '1.0.0', run: async function(params, ctx) { return { ok: true, action: 'shutdown', delaySec: Number(params.delaySec || 0), at: new Date().toISOString() }; } };
