var restartPc = { id: 'restart-pc', version: '1.0.0', run: async function(params, ctx) { return { ok: true, action: 'restart', delaySec: Number(params.delaySec || 0), at: new Date().toISOString() }; } };
